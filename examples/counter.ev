package main

fn main() {
    counter := makeCounter()
    echo counter() // 1
    echo counter() // 2
    echo counter() // 3
}

fn makeCounter() {
    var count := 0

    return fn() {
        count += 1
        return count
    }
}