package main

fn main() {
    //echo "Hello World"
    example()
}

fn example() {
    //a := 5 + "10" // crash the program
    //echo a        // we don't make it here

    b := catch(5 + "10")

    // handle the error
    if b is error {
        echo "b is definitely an error"
    }
}