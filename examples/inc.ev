package main

var x = 0

fn inc(n: int) {
    while n > 0 {
        x += 1
        n -= 1
    }
}

fn main() {
    let a = go inc(10000000)
    let b = go inc(10000000)

    await.all (a, b)
    echo x
}
