package main imports("time")

var x := 0

fn inc(n) {
    while (n > 0) {
        x += 1
        n -= 1
    }
}

fn main() {
    go inc(10000)
    go inc(10000)

    await time.timer(10) // yield for 10ms
    echo x
}