package main

var x := 0

fn inc(n) {
    while n > 0 {
        x += 1
        n -= 1
    }
}

// go run ./cli -t ./examples/inc.ev
fn main() {
    a := go inc(10000000)
    b := go inc(10000000)

    await.all (a, b)
    echo x
}