package main imports("io")

fn fib(n) unsynced {
    if n < 2 {
        return n
    }
    return fib(n-1) + fib(n-2)
}

fn main() {
    a := go fib(35)
    b := go fib(35)
    c := go fib(35)

    io.println(await.all(a, b, c))
}